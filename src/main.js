import { createApp, h } from "vue"
import App from "./App.vue"

// createApp({
//     render() {
//         return h("div", "hello from kvite")
//     }
// }).mount("#app")

createApp(App).mount("#app")